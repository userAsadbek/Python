#Creating function #
#We use def to define create function
def hello(to="world"):
    print("Hello,",to)


#def hello(to="world"): this default argument
def main():
    name=input("Enter your name: ")
    hello(name)
    x=int(input("What's x? "))
    print("x  squared is",square(x))

def square(n):
    return n*n
    #to power 2 is n**2
    #or using pow as c++

main()
