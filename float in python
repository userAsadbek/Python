#float
a=float(input("enter the first number"))
b=float(input("enter the second number"))
print(a+b) #gives just adding them

#Rounding number to 0. zeroes
#round(number[,ndigits])
c=round(a+b) #round to integer

d=round(a/b,2) #show 2 numbers after decimal
print(f"{d:.2f}")
